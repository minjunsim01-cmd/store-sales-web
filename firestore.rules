rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP용 단순 규칙입니다. 직원 10명 미만 내부 사용 기준.
    // 실제 운영 후에는 관리자 권한 기반 규칙으로 강화하는 것을 권장합니다.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
